name: Deploy on card column
on:
  issues:
    types: [edited, labeled]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Read card
      run: |
        echo ${{ github.event.label.name }}
